Holehe is an OSINT tool that checks if an email is registered on websites via password-reset flows. This guide covers Windows installation, pip setup, commonly missed dependencies (requests, httpx, trio, bs4), fixing PATH issues, CLI usage, and troubleshooting.

Installation Guide
A complete walkthrough for installing and running Holehe on Windows, including:
- Prerequisites and recommended Python version
- Installing Holehe via pip
- Commonly missed dependencies (`requests`, `httpx`, `trio`, `bs4`) and why each is needed
- Verifying the installation
- Fixing "command not recognized" errors
- Full CLI usage examples (`--only-used`, `-C`, `-NP`, `-T`)
- Command-line flags reference table
- Troubleshooting checklist and virtual environment setup
